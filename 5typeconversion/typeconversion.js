/**
 * type conversion is used to convrt a varibles type
 * ex:if we want to change string to number or boolean vice-versa  their we use type converion
 * 
 */

var str="prakash";
console.log(typeof(str)); //string

//if we want to change string to number

let num=Number(str)
console.log(num)// NaN

//if we want to change it boolean

let bol=Boolean(num);
console.log(bol)//true





//indexOf() method is used for find  index of element
 //includes() used the writen line linked with which languqage(Javascript,Html)